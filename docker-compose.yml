version: "3.9"

services:
  api:
    build: .
    container_name: course_completion_api
    ports:
      - "8000:8000"
    # If you want auto-reload only in dev you can mount local code:
    # volumes:
    #   - .:/app
    # command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
